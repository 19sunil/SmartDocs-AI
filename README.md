# SmartDocsAI
SmartDocs AI is a semantic search system that retrieves documents based on meaning instead of keywords. It uses transformer embeddings to convert text into vectors and performs similarity search to find relevant results. The project demonstrates an AI workflow using embeddings and vector databases like Endee for scalable semantic retrieval.
